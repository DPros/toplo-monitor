{
  "name": "toplo-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hot-water/heating outage monitor for Toplofikatsia Sofia — service rewrite (Next.js + TS).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
