/** @type {import('next').NextConfig} */
const nextConfig = {
  // The matching engine is plain TS under src/lib/engine and needs no special
  // handling. Add config here as the app grows.
};

export default nextConfig;
